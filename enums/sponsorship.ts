export enum SponsorshipStage {
  LEAD = "Planned",
  OUTREACH = "Shooting",
  NEGOTIATION = "Editing",
  SIGNED = "Review",
  PAID = "Published",
}

export enum DealPriority {
  HIGH = "high",
  MEDIUM = "medium",
  LOW = "low",
}

export enum SponsorshipMode {
  TABLE = "table",
  KANBAN = "kanban",
  Analytics = "analytics",
}
